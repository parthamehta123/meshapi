"""FastAPI demo that answers Harbor Desk questions through MeshAPI only.

Chat, managed RAG upload/search, moderation, STT, and TTS all share one client
and one API key. Run with:

    uvicorn native_rag_app.main:app --reload
"""
